import { ControlIdPipe } from './control-id.pipe';

describe('ControlIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlIdPipe();
    expect(pipe).toBeTruthy();
  });
});
